// Array of names
const names = ["Anya", "Bebo", "dhani", "Dhruv","shreyshri", "Esha", "Faiz", "Gaurav"];

// Print names with length greater than 5
names.forEach(name => {
    if (name.length > 5) {
        console.log(name);
    }
});

